version: 1.0
runtime: python311
build:
  commands:
    pre-build:
      - pip install --upgrade pip
    build:
      - pip install --no-cache-dir -r requirements.txt
run:
  command: python app.py
  network:
    port: 8080
    env: PORT
  env:
    - name: PYTHONPATH
      value: "."
